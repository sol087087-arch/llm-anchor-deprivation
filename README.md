# semantic-inertia-experiments
Empirical protocol testing semantic basin stability across 15 LLMs (3 tiers). Progressive anchor removal + lexical constraint pressure reveals semantic persistence dynamics. 10-step conversational design with A/B framing to isolate model behavior from eval artifact. 2700 clean runs.
